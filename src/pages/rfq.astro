---
import BaseLayout from "@layouts/BaseLayout.astro";
import Section from "@components/ui/Section.astro";
import Card from "@components/ui/Card.astro";
import RFQForm from "@components/react/RFQForm";
import { Image } from "astro:assets";
import {
  FileText,
  Clock,
  Shield,
  CheckCircle2,
  Phone,
  Mail,
  MessageCircle,
  Award,
  TrendingUp,
  Users,
  MapPin,
} from "lucide-react";
import rfqBgImg from "@assets/photo-1558618666-fcd25c85cd64.jpg";
import warehouseImg from "@assets/photo-1563013544-824ae1b704d3.jpg";
import { SITE, SOCIAL_LINKS } from "@config/site";
---

<BaseLayout title="Solicitar Cotización - Garola Inoxidable">
  <!-- Hero Section with Professional Image -->
  <section
    class="relative h-[500px] md:h-[600px] flex items-center overflow-hidden"
  >
    <!-- Background Image with Overlay -->
    <div class="absolute inset-0">
      <Image
        src={rfqBgImg}
        alt="Ingeniería industrial y fabricación en acero inoxidable"
        class="w-full h-full object-cover"
        width={2070}
        height={1380}
      />
      <div
        class="absolute inset-0 bg-linear-to-r from-[#14171A]/95 via-[#1a1f24]/90 to-[#14171A]/85"
      >
      </div>
    </div>

    <!-- Content -->
    <div class="container mx-auto relative z-10 px-4 sm:px-6 lg:px-8">
      <div class="max-w-4xl mx-auto text-center">
        <div
          class="inline-flex items-center px-4 py-2 mb-6 bg-red-600/10 text-red-500 text-sm font-semibold rounded-full border border-red-600/20 uppercase tracking-widest animate-fadeInUp"
        >
          <FileText className="w-4 h-4 mr-2" />
          Comience su Proyecto Hoy
        </div>

        <h1
          class="text-4xl md:text-6xl lg:text-7xl font-bold text-white mb-6 leading-tight animate-fadeInUp"
          style="animation-delay: 0.1s"
        >
          Solicitar <span
            class="text-transparent bg-clip-text bg-linear-to-r from-red-500 to-red-800"
            >Cotización</span
          >
        </h1>

        <p
          class="text-xl md:text-2xl text-slate-300 mb-8 max-w-3xl mx-auto leading-relaxed animate-fadeInUp"
          style="animation-delay: 0.2s"
        >
          Obtenga una propuesta personalizada adaptada a sus necesidades
          específicas de industria láctea o alimenticia. Respondemos en menos de
          24 horas hábiles.
        </p>

        <!-- Quick Stats -->
        <div
          class="grid grid-cols-2 md:grid-cols-4 gap-6 pt-8 border-t border-slate-700 animate-fadeInUp"
          style="animation-delay: 0.3s"
        >
          <div class="group">
            <div
              class="text-3xl md:text-4xl font-bold text-white mb-1 group-hover:text-red-500 transition-colors"
            >
              24hs
            </div>
            <div class="text-sm text-slate-400 uppercase tracking-wider">
              Tiempo Respuesta
            </div>
          </div>
          <div class="group">
            <div
              class="text-3xl md:text-4xl font-bold text-white mb-1 group-hover:text-red-500 transition-colors"
            >
              120+
            </div>
            <div class="text-sm text-slate-400 uppercase tracking-wider">
              Proyectos
            </div>
          </div>
          <div class="group">
            <div
              class="text-3xl md:text-4xl font-bold text-white mb-1 group-hover:text-red-500 transition-colors"
            >
              100%
            </div>
            <div class="text-sm text-slate-400 uppercase tracking-wider">
              Calidad
            </div>
          </div>
          <div class="group">
            <div
              class="text-3xl md:text-4xl font-bold text-white mb-1 group-hover:text-red-500 transition-colors"
            >
              ISO
            </div>
            <div class="text-sm text-slate-400 uppercase tracking-wider">
              Certificado
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Main Form Section -->
  <Section background="dark">
    <div class="gap-12">
      <!-- Form -->
      <div class="lg:col-span-2">
        <Card class="bg-[#1a1f24] border-slate-800">
          <RFQForm client:load />
        </Card>
      </div>
    </div>
  </Section>

  <!-- Trust & Success Section -->
</BaseLayout>

<style>
  .bg-dark {
    background-color: #14171a;
  }
  .bg-gray {
    background-color: #1a1f24;
  }
</style>
